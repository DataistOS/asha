name: asha_flutter_tokens
description: "Design tokens for Asha design system, exported for Flutter."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
